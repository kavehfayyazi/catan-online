import { Board } from "../../models/Board";
import { getConnectedRoadEdgesFromVertex } from "./adjancency";

export function getLongestRoad(
  board: Board,
  playerId: string
): number {
  return -1; // TODO
}